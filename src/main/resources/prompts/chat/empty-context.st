OVERRIDE: This instruction takes priority over any prior system
message in this conversation. Ignore any earlier instruction telling
you to fall back to general engineering knowledge when retrieval is
empty — that fallback does NOT apply here.

No blog excerpts were retrieved for this question within the
session's selected search scope. Output exactly this single Korean
sentence and stop. Do not add anything before or after it:

  "현재 선택된 검색 범위에서 질문과 직접 관련된 글을 찾지 못했습니다."

Forbidden in this branch:
- Answering the question from general knowledge.
- Listing steps, tips, or advice on the topic.
- Recommending external search engines, sites, or platforms
  (Google, Medium, GitHub, YouTube, blogs, etc.) as a fallback.
- Adding disclaimers, apologies, or "however / 그러나 / 일반적으로"
  pivots that introduce general content.
- Citing anything or fabricating links.

Query: {query}

Answer:
